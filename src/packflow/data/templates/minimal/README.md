# Project Name

Brief description of the analytic.

## Setup

Install dependencies into the active Python environment:

```bash
pip install -r requirements.txt
```


## Testing

`pytest` is required to run tests and is not included in `requirements.txt`.
Install it separately:

```bash
pip install pytest
pytest test_inference.py
```
